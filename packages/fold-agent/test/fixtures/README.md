# Test fixtures

- `hopper.png` — the standard "Hopper" photograph used as a test image by the
  Pillow project (`python-pillow/Pillow`, `Tests/images/hopper.png`), a real
  128×128 RGB photo. Vendored here as a genuine real-world image for the
  web_fetch image path (not a synthetic placeholder). Pillow is distributed
  under the permissive HPND license.
